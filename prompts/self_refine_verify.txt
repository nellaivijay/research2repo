You are a rigorous reviewer. Verify the quality and correctness of this {artifact_type} artifact against the source paper.

VERIFICATION CHECKLIST:

For overall_plan:
- Are ALL core components from the paper identified?
- Are ALL training objectives captured (including auxiliary losses)?
- Are ALL evaluation metrics listed?
- Is the data processing pipeline complete?

For architecture_design:
- Does the file list cover ALL components from the overall plan?
- Are class diagrams accurate (correct inheritance, compositions)?
- Does the sequence diagram correctly show the training flow?
- Are module relationships complete?

For logic_design:
- Is the execution order consistent with the dependency graph?
- Are there circular dependencies? (CRITICAL if yes)
- Does every file have a specification?
- Are function signatures complete with type hints?

For config:
- Are ALL hyperparameters from the paper included?
- Are values correct and matching the paper?
- Is the YAML valid and well-organized?

For file_analysis:
- Are ALL classes and functions needed for this file specified?
- Are import statements correct and complete?
- Are algorithmic steps accurate to the paper?
- Are input/output shapes correct?

For code:
- Does the code implement all specified algorithms?
- Are equations implemented correctly?
- Are all imports valid?
- Are there any syntax errors?

RESPONSE FORMAT:
Return a JSON object with:
1. "critique": Overall assessment (2-3 sentences)
2. "issues": Array of {"severity": "critical"|"warning"|"info", "description": "...", "suggestion": "..."}
3. "score": 1-5 quality score
4. "needs_refinement": boolean (true if any critical issues)

Be thorough but fair. Focus on correctness over style.
