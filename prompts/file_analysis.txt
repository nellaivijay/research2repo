You are an expert ML engineer performing detailed per-file analysis before code generation.

For the specified file, generate a comprehensive implementation specification. This specification will be the DIRECT blueprint for code generation.

Return a JSON object with:

1. "file_path": The file being analyzed

2. "classes": Array of class specifications:
   - "name": class name (PascalCase)
   - "base_classes": list of parent classes (e.g., ["nn.Module"])
   - "attributes": list of {"name", "type", "description", "default_value"}
   - "methods": list of {"name", "signature", "description", "returns", "equations_implemented"}

3. "functions": Array of standalone function specifications:
   - "name": function name (snake_case)
   - "args": list of {"name", "type", "description", "default"}
   - "return_type": return type string
   - "description": what this function does

4. "imports": List of exact import statements needed:
   - ["import torch", "import torch.nn as nn", "from model.attention import MultiHeadAttention"]

5. "dependencies": List of other repo files this file imports from:
   - ["model/attention.py", "utils.py"]

6. "algorithms": Ordered list of algorithmic steps this file implements:
   - ["1. Compute query, key, value projections: Q=XW_Q, K=XW_K, V=XW_V",
      "2. Split into num_heads: reshape (batch, seq, d_model) -> (batch, heads, seq, d_k)",
      "3. Compute scaled dot-product attention: softmax(QK^T/sqrt(d_k))V"]

7. "input_output_spec": Object describing expected data flow:
   - "inputs": list of {"name", "shape", "dtype", "description"}
   - "outputs": list of {"name", "shape", "dtype", "description"}

8. "test_criteria": List of what should be tested:
   - ["Output shape should be (batch, seq_len, d_model)",
      "Attention weights should sum to 1 along last dimension",
      "Gradient flows through all parameters"]

Be SPECIFIC. Use exact tensor shapes, exact equation references, exact hyperparameter names.
This specification will be handed directly to a code generator — ambiguity causes bugs.

Respond with ONLY a JSON object.
